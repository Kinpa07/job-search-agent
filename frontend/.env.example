# Frontend env template (Module 7+). Copy to frontend/.env.
# Vite only exposes vars prefixed VITE_ to client code (import.meta.env.VITE_*).
# NEVER put backend secrets here — this file is bundled into browser-shipped JS.

# Backend API base URL
VITE_API_BASE_URL=http://localhost:8000
